{
  "access_continuity_justification": "Continuity rests on the steward organisation, not one person: the repository is owned by the `presidio-v` GitHub org (not a personal account); PyPI publishing uses Trusted Publishing (OIDC) bound to the org repo, so there is no personal API token that dies with an individual; the release signing key is held in the organisation password manager and is recoverable; and the release procedure is written down in `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/PUBLISHING.md`. A second person with write access is in place: `ceoofcyber` is a write collaborator and a code owner, so more than one individual can assume each role. URL: `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/GOVERNANCE.md#project-continuity`.",
  "access_continuity_status": "Met",
  "accessibility_best_practices_justification": "Confirmed N/A: this is a headless stdio MCP server with no graphical or end-user interface.",
  "accessibility_best_practices_status": "N/A",
  "assurance_case_justification": "URL: `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/ASSURANCE.md`. Consolidated assurance case with all four required parts (threat model, trust boundaries, secure-design-principles argument, common-implementation-weakness argument). Confirmed: `ASSURANCE.md` is fully written for this project \u2014 no template placeholders remain \u2014 and its threat model states what is out of scope (the parent library's internals, key custody, detector recall, the payment rail) rather than leaving those silently assumed.",
  "assurance_case_status": "Met",
  "automated_integration_testing_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/.github/workflows/ci.yml` runs the full suite on every push and pull request. `ci.yml` runs the full suite on every push and pull request across a **Python 3.10 / 3.11 / 3.12 / 3.13 matrix** on Linux, plus lint, lockfile-drift, dependency audit, Bandit, CodeQL, Gitleaks and an Atheris fuzz job. There is no partner or external end-to-end suite; `tests/test_e2e_inprocess_client.py` drives the server in-process through a real MCP client.",
  "automated_integration_testing_status": "Met",
  "build_common_tools_justification": "Built with common, widely available tools. Python, `hatchling` as the PEP 517 backend, and `uv` \u2014 all common and freely available.",
  "build_common_tools_status": "Met",
  "build_floss_tools_justification": "The entire toolchain is FLOSS.",
  "build_floss_tools_status": "Met",
  "build_justification": "Standard PEP 517 build from source: `python -m build`, or `uv build`. The release workflow builds the sdist and wheel this way before publishing.",
  "build_non_recursive_justification": "Confirmed N/A: no make or recursive subdirectory build.",
  "build_non_recursive_status": "N/A",
  "build_preserve_debug_justification": "Confirmed N/A: no compiled artefacts.",
  "build_preserve_debug_status": "N/A",
  "build_repeatable_justification": "Artefacts are built via the standard flow against a fully pinned dependency graph on GitHub-hosted runners with SHA-pinned Actions; the build is deterministic from pinned sources. Confirmed pinned, and deliberately **not** claimed as bit-for-bit reproducible. `uv.lock` pins the full dependency graph; every GitHub Action is pinned to a commit SHA with the tag in a trailing comment; builds run on GitHub-hosted runners via a standard PEP 517 build. Hermetic, byte-identical reproducibility has not been verified and is not asserted.",
  "build_repeatable_status": "Met",
  "build_standard_variables_justification": "Confirmed N/A: pure Python, no compiler or linker step.",
  "build_standard_variables_status": "N/A",
  "build_status": "Met",
  "bus_factor_justification": "Bus factor is backed by the PRESIDIO organisation rather than a lone maintainer. More than one person can assume the maintainer, security-contact and release-manager roles: an external reviewer (`ceoofcyber`) holds write access and is named in `.github/CODEOWNERS`, and PRESIDIO is a staffed organisation with the depth to backfill each role. All release credentials are organisation-held and recoverable from an enterprise vault rather than tied to one machine or one person \u2014 the signing key, the deployment secrets, and the publishing path (Trusted Publishing via OIDC bound to the org repo, so no personal API token exists to be lost). Issue triage, change acceptance and releases can therefore continue if any single individual becomes unavailable. See `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/GOVERNANCE.md#project-continuity`.",
  "bus_factor_status": "Met",
  "code_of_conduct_justification": "Contributor Covenant at `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CODE_OF_CONDUCT.md` (standard location).",
  "code_of_conduct_status": "Met",
  "coding_standards_enforced_justification": "The style/lint check runs in CI on every PR (FLOSS enforcement).",
  "coding_standards_enforced_status": "Met",
  "coding_standards_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CONTRIBUTING.md#style` names the required style/lint tool; config in the project's build/lint config. The tool is **ruff**; configuration in `[tool.ruff]` in `pyproject.toml`.",
  "coding_standards_status": "Met",
  "contribution_justification": "URL: `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CONTRIBUTING.md` \u2014 documents the fork \u2192 branch \u2192 PR flow against `main`.",
  "contribution_requirements_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CONTRIBUTING.md#requirements-for-acceptable-contributions` \u2014 style, tests, security-change rules, dependency bar.",
  "contribution_requirements_status": "Met",
  "contribution_status": "Met",
  "crypto_algorithm_agility_justification": "Confirmed N/A: this package negotiates no cipher suites of its own. TLS suite selection is delegated to `httpx`/OpenSSL, which follow the platform's current defaults.",
  "crypto_algorithm_agility_status": "N/A",
  "crypto_call_justification": "N/A: no cryptographic primitive is re-implemented or called here. Transport security comes from `httpx`/OpenSSL; the parent library uses Python's `hmac`/`hashlib` and `cryptography`.",
  "crypto_call_status": "Met",
  "crypto_certificate_verification_justification": "TLS certificate verification is on by default; verification is not disabled. Confirmed: `httpx` verifies certificates by default and this package never passes `verify=False` or supplies a permissive SSL context.",
  "crypto_certificate_verification_status": "Met",
  "crypto_credential_agility_justification": "Confirmed: every credential \u2014 `PRESIDIO_X402_MCP_REMOTE_API_KEY`, `PRESIDIO_X402_FINGERPRINT_KEY`, `PRESIDIO_X402_CHAIN_KEY` \u2014 is supplied from outside the source tree through environment variables and is rotatable by restarting the process. None is hard-coded; Gitleaks gates this on every push.",
  "crypto_credential_agility_status": "Met",
  "crypto_floss_justification": "The crypto libraries used are FLOSS. Confirmed: OpenSSL, and the parent library's `cryptography` \u2014 both FLOSS.",
  "crypto_floss_status": "Met",
  "crypto_keylength_justification": "N/A here \u2014 no keys are generated or chosen by this package. TLS key sizes follow the platform defaults, and the parent library's Ed25519 and SHA-256 choices exceed NIST's 2030 minimums.",
  "crypto_keylength_status": "Met",
  "crypto_password_storage_justification": "Stores no external-user passwords. Confirmed: no external-user passwords are stored.",
  "crypto_password_storage_status": "N/A",
  "crypto_pfs_justification": "Implements no key-agreement protocol of its own; transport PFS is provided by TLS in the network layer. Confirmed: no key-agreement protocol of its own; PFS comes from the TLS layer.",
  "crypto_pfs_status": "N/A",
  "crypto_published_justification": "N/A for this package: it implements no security function and calls no hash or cipher primitive. TLS is used for transport; the parent library performs HMAC-SHA256 chaining and Ed25519 signing.",
  "crypto_published_status": "Met",
  "crypto_random_justification": "N/A: this package generates no security-relevant randomness \u2014 it contains no `random`, `secrets`, or key-generation call. Replay fingerprints and audit-chain keys are the parent library's responsibility.",
  "crypto_random_status": "Met",
  "crypto_tls12_justification": "The HTTP client uses TLS \u22651.2. Confirmed: `httpx` on the platform OpenSSL negotiates TLS 1.2 or better; this package does not lower the default minimum version.",
  "crypto_tls12_status": "Met",
  "crypto_used_network_justification": "Network communication uses TLS. Confirmed. The single outbound call is HTTPS, and a non-TLS base URL now refuses to start (loopback excepted, where traffic does not cross a network).",
  "crypto_used_network_status": "Met",
  "crypto_verification_private_justification": "Certificate verification precedes transmission of any private data. Confirmed: the pre-redaction payload and the `X-API-Key` header are only sent inside the verified TLS session established by `httpx.AsyncClient`, after certificate verification.",
  "crypto_verification_private_status": "Met",
  "crypto_weaknesses_justification": "N/A for this package: it implements no security functions of its own and contains no call to any hash or cipher primitive. Cryptography (HMAC-SHA256 chaining, Ed25519 signing) belongs to the parent library. Transport security is TLS via `httpx`.",
  "crypto_weaknesses_status": "Met",
  "crypto_working_justification": "No MD4, MD5, single DES, RC4, or Dual_EC_DRBG. Confirmed: none of these appear anywhere in this codebase.",
  "crypto_working_status": "Met",
  "dco_justification": "Every commit must carry a DCO `Signed-off-by` line (`git commit -s`); enforced in review. Documented at `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CONTRIBUTING.md#licensing-and-developer-certificate-of-origin-dco`. Inbound = outbound `MIT`.",
  "dco_status": "Met",
  "delivery_mitm_justification": "Distributed over HTTPS via the package index and GitHub. Published to PyPI through **Trusted Publishing (OIDC)** from the org-owned repository, so no long-lived API token exists to steal. Release tags are SSH-signed and shown as Verified on GitHub, and the verifying public key is committed in `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/allowed_signers`. Distribution is HTTPS throughout.",
  "delivery_mitm_status": "Met",
  "delivery_unsigned_justification": "No hash is fetched over plain HTTP. Release tags are SSH-signed and GitHub-verified.",
  "delivery_unsigned_status": "Met",
  "dependency_monitoring_justification": "Dependabot + dependency audit in CI + OpenSSF Scorecard continuously check external dependencies for known vulnerabilities.",
  "dependency_monitoring_status": "Met",
  "description_good_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp#readme` \u2014 README opens with what `presidio-hardened-x402-mcp` does and the problem it solves. Confirmed.",
  "description_good_status": "Met",
  "discussion_justification": "GitHub Issues: `https://github.com/presidio-v/presidio-hardened-x402-mcp/issues` \u2014 searchable, URL-addressable, open, no proprietary client.",
  "discussion_status": "Met",
  "documentation_achievements_justification": "The OpenSSF Best Practices badge is displayed and hyperlinked on the README front page.",
  "documentation_achievements_status": "Met",
  "documentation_architecture_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/ARCHITECTURE.md` \u2014 components, core-flow pipeline, and trust boundaries; linked from the README.",
  "documentation_architecture_status": "Met",
  "documentation_basics_justification": "README plus `docs/`. The README is the documentation: installation, MCP host configuration, the three tools and their arguments, both operating modes, and the full environment-variable table. There is no separate `docs/` tree.",
  "documentation_basics_status": "Met",
  "documentation_current_justification": "Docs track the current release line; per-version roadmap and hand-written `CHANGELOG.md` are kept in sync with each release.",
  "documentation_current_status": "Met",
  "documentation_interface_justification": "README API section; the public interface is documented and, for a library, enumerated in `SEMVER.md` (`presidio_x402_mcp.__all__` or equivalent). The external interface is the MCP tool surface \u2014 three tools, their arguments and result shapes \u2014 documented in the README and defined as the public API in `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SEMVER.md`. Note that `SEMVER.md` states the Python module is *not* a supported import surface; the server is consumed over MCP.",
  "documentation_interface_status": "Met",
  "documentation_quick_start_justification": "README \"Quick Start\" (before/after example) plus `docs/`. README \"Quick start\" shows the MCP host configuration and the three tool calls an agent makes; `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CONTRIBUTING.md#local-verification` covers the development path. There is no separate `docs/` tree \u2014 the README is the quick-start material.",
  "documentation_quick_start_status": "Met",
  "documentation_roadmap_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp#roadmap` includes a \"Planned direction (next 12 months)\" section. Confirmed: the README roadmap names real, dated intended work.",
  "documentation_roadmap_status": "Met",
  "documentation_security_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SECURITY.md` documents the security controls, threat model, and reporting process; `ARCHITECTURE.md#trust-boundaries` states the trust boundaries. The full threat model, trust boundaries, secure-design argument and weakness-class argument live in `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/ASSURANCE.md`; the audit history, including findings that remain open, is in `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SECURITY-AUDIT.md`.",
  "documentation_security_status": "Met",
  "dynamic_analysis_enable_assertions_justification": "The suite is assertion-based; assertions stay enabled in tests. Confirmed: assertions are enabled during both the test suite and the fuzz job \u2014 Python is invoked without `-O` and `PYTHONOPTIMIZE` is unset, so the harness's invariant assertions are actually evaluated.",
  "dynamic_analysis_enable_assertions_status": "Met",
  "dynamic_analysis_fixed_justification": "No unfixed medium+ findings.",
  "dynamic_analysis_fixed_status": "Met",
  "dynamic_analysis_justification": "An Atheris coverage-guided fuzz harness (`fuzz/fuzz_config_validation.py`) runs in CI on every pull request, time-boxed, under Python 3.12 on Linux. It drives the two configuration parsers: `_validate_remote_base_url` and the per-endpoint policy JSON parsing. The URL target asserts a **security invariant** \u2014 anything accepted must be https or loopback \u2014 rather than only checking for crashes, because a validator that accepts `http://evil.example.com` without raising is exactly the defect the gate exists to prevent.",
  "dynamic_analysis_status": "Met",
  "dynamic_analysis_unsafe_justification": "Confirmed N/A: Python is memory-safe and this package contains no native code. Note that coverage-guided fuzzing runs regardless \u2014 an Atheris harness (`fuzz/fuzz_config_validation.py`) exercises the configuration validators on every pull request.",
  "dynamic_analysis_unsafe_status": "N/A",
  "english_justification": "All docs and issue handling in English.",
  "english_status": "Met",
  "enhancement_responses_justification": "No enhancement requests have been received. See the note on `report_responses` \u2014 the only tracker item is a promotional post.",
  "enhancement_responses_status": "Met",
  "external_dependencies_justification": "Dependencies are listed machine-readably in the project manifest and fully pinned in a lockfile; a CycloneDX SBOM is generated per release in CI. Manifest `pyproject.toml`; lockfile `uv.lock`, pinning all 87 resolved packages. CI enforces the two stay in step via a `uv lock --locked` drift check on every PR.",
  "external_dependencies_status": "Met",
  "floss_license_justification": "`MIT`.",
  "floss_license_osi_justification": "`MIT` is OSI-approved. Confirmed: MIT is on the OSI approved list.",
  "floss_license_osi_status": "Met",
  "floss_license_status": "Met",
  "governance_justification": "Governance model documented at `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/GOVERNANCE.md` \u2014 decision-making, escalation, and security/API change rules. Confirmed against the file.",
  "governance_status": "Met",
  "hardening_justification": "TLS is required on the only outbound endpoint, verified at import and refusing to start otherwise; certificate verification is on by default and never disabled; requests are time-boxed. Every GitHub Action is SHA-pinned. Workflow tokens are least-privilege. Gitleaks scans the full history and secret-scanning push protection is enabled on the repository. Secrets are read from the environment and never logged; diagnostics go to stderr so they cannot corrupt the stdio protocol channel.",
  "hardening_status": "Met",
  "implement_secure_design_justification": "**Fail-safe defaults:** the default mode is fully local with no network egress; both gates raise rather than returning a permissive result; a cleartext remote endpoint prevents startup. **Complete mediation:** every tool call passes length validation before processing, and the gates record as they check, so no caller observes an allow decision that was not also accounted for. **Least privilege:** the package holds no long-lived secret it does not need, and CI workflows declare `contents: read` at the top level with only CodeQL's SARIF upload and the release job's OIDC token re-declaring more. **Defence in depth:** three tools counter distinct threats and do not substitute for one another; Bandit and CodeQL analyse the same code differently. **Economy of mechanism:** no cryptography is implemented here at all, and the adapter is small enough to read end to end. See `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/ASSURANCE.md#3-secure-design-principles-applied`.",
  "implement_secure_design_status": "Met",
  "input_validation_justification": "All data crossing the untrusted boundary is validated against a whitelist before use. Concrete example: `_validate_lengths` caps `resource_url` at 2048 and `description` / `reason` at 4096 characters before any processing, mirroring the screening service's request model so both entry points reject the same oversized input; the metadata is then screened by the parent `PIIFilter`. Separately, `_validate_remote_base_url` whitelists the `https` scheme (loopback excepted) and matches on the parsed hostname, so `http://localhost.evil.example.com` is refused.",
  "input_validation_status": "Met",
  "installation_common_justification": "Standard install from the package index. `pip install presidio-hardened-x402-mcp`, or run it directly with `uvx presidio-hardened-x402-mcp` \u2014 the documented MCP host invocation.",
  "installation_common_status": "Met",
  "installation_development_quick_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CONTRIBUTING.md#local-verification` \u2014 documents the one setup path that installs everything needed to build and test.",
  "installation_development_quick_status": "Met",
  "installation_standard_variables_justification": "Confirmed N/A: installed via pip/uv from PyPI, so `DESTDIR`-style conventions do not apply.",
  "installation_standard_variables_status": "N/A",
  "interact_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp#readme` \u2014 README covers obtaining (presidio_x402_mcp on the package index), feedback (issues), security reports (`SECURITY.md`), and contributing.",
  "interact_status": "Met",
  "interfaces_current_justification": "Dependencies are kept current (Dependabot + dependency floors), the public API is tracked in `SEMVER.md`, and the code does not rely on deprecated FLOSS functions where alternatives exist.",
  "interfaces_current_status": "Met",
  "internationalization_justification": "Confirmed N/A: no user-facing localizable strings; the only output is a JSON-RPC tool result consumed by an agent.",
  "internationalization_status": "N/A",
  "know_common_errors_justification": "The documented threat model (`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/ASSURANCE.md#1-threat-model`) names the classes defended and maps each to a control: personal data leaking to a counterparty, an agent driven to overspend, duplicate submission, cleartext egress from a misconfigured endpoint, a remote failure mistaken for a clean result, audit-trail tampering, and protocol-channel corruption. Section 4 maps CWE classes \u2014 improper input validation (CWE-20/74), insecure transport and SSRF (CWE-319/295), unsafe deserialization (CWE-502), exposed secrets (CWE-798/532), vulnerable dependencies (CWE-1104) \u2014 each to its control and to the tool that checks it.",
  "know_common_errors_status": "Met",
  "know_secure_design_justification": "The maintainer designs to these principles and the codebase shows it: fail-closed defaults (both gates raise rather than returning permissive results; a cleartext endpoint stops startup instead of downgrading), least privilege (least-privilege CI tokens; no long-lived secret held that is not needed), defence in depth (three independent controls, plus two SAST tools), complete mediation (validation before any processing, on every call), and economy of mechanism (no bespoke cryptography \u2014 none is implemented here at all). The reasoning is written out in `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/ASSURANCE.md#3-secure-design-principles-applied`.",
  "know_secure_design_status": "Met",
  "license_location_justification": "URL: `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/LICENSE`",
  "license_location_status": "Met",
  "maintained_justification": "Actively maintained. Actively maintained: **v0.1.3 released 2026-08-02**, carrying a parent security floor and a fix for an open audit finding, with four pull requests merged that day.",
  "maintained_status": "Met",
  "maintenance_or_update_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SECURITY.md#supported-versions` states which versions are supported and for how long; `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SEMVER.md` documents the upgrade path and what counts as a breaking change.",
  "maintenance_or_update_status": "Met",
  "no_leaked_credentials_justification": "**Verified.** No `.env`, `.pem`, key, keystore or credential-shaped file appears anywhere in the repository's history \u2014 checked across all refs. Gitleaks scans the full history (`fetch-depth: 0`) on every push and pull request, and secret-scanning push protection is enabled on the repository.",
  "no_leaked_credentials_status": "Met",
  "regression_tests_added50_justification": "Policy requires a regression test with every bug fix. Worked example: PR #31 raised the parent floor after a redaction bypass and shipped four `TestPercentEncodedPII` cases **verified to fail against the previous parent** \u2014 a genuine regression test, not a restatement of current behaviour. PR #32 fixed the missing TLS check on the remote endpoint and shipped 19 tests in the same change. Both bug fixes in the last six months carry regression tests, so the >50% bar is met on a small but complete sample.",
  "regression_tests_added50_status": "Met",
  "release_notes_justification": "URL: `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CHANGELOG.md` \u2014 Keep a Changelog format, hand-written, not VCS log output.",
  "release_notes_status": "Met",
  "release_notes_vulns_justification": "CHANGELOG names each CVE/GHSA fixed. No CVE or GHSA identifier has ever applied to this project, so there is nothing of that kind to name. The CHANGELOG does document security-relevant fixes explicitly: the 0.1.3 entry describes the parent percent-encoding redaction bypass and the reason the previous version bound hid it, and names the audit findings closed (1, 2 and 8).",
  "release_notes_vulns_status": "Met",
  "repo_distributed_justification": "git.",
  "repo_distributed_status": "Met",
  "repo_interim_justification": "Feature and fix branches are pushed between releases; PR-based flow.",
  "repo_interim_status": "Met",
  "repo_public_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp`",
  "repo_public_status": "Met",
  "repo_track_justification": "git.",
  "repo_track_status": "Met",
  "report_archive_justification": "URL: `https://github.com/presidio-v/presidio-hardened-x402-mcp/issues?q=is%3Aissue` \u2014 public and searchable.",
  "report_archive_status": "Met",
  "report_process_justification": "URL: `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CONTRIBUTING.md#reporting-bugs-and-requesting-features`",
  "report_process_status": "Met",
  "report_responses_justification": "The project has received **no bug reports** to date. The single issue in the tracker (#30, opened 2026-07-31) is an unsolicited promotional post advertising a third-party analytics service, not a report or an enhancement request, and it has not been answered. The criterion is therefore satisfied vacuously rather than by a record of responses, and it is stated that way so a reviewer can judge it.",
  "report_responses_status": "Met",
  "report_tracker_justification": "GitHub Issues: `https://github.com/presidio-v/presidio-hardened-x402-mcp/issues`.",
  "report_tracker_status": "Met",
  "roles_responsibilities_justification": "Key roles (steward org, maintainer, security contact, release manager, contributor) documented at `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/GOVERNANCE.md#roles-and-responsibilities`.",
  "roles_responsibilities_status": "Met",
  "signed_releases_justification": "Releases are cryptographically signed and the process for obtaining/verifying keys is documented at `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SECURITY.md#obtaining-the-public-signing-keys`: build provenance attestation, SSH-signed git tags with the public key in `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/allowed_signers`. Confirmed for this project: release tags are SSH-signed with the org key and show as Verified on GitHub (v0.1.3 verified `reason: valid`); the public key is in `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/allowed_signers`. PyPI artefacts are published through Trusted Publishing (OIDC), so no long-lived token is involved.",
  "signed_releases_status": "Met",
  "sites_https_justification": "GitHub and the package index are HTTPS. Confirmed: the project runs no hosted site of its own. GitHub, PyPI and the MCP registry entry are all HTTPS. The optional remote screening endpoint an operator may configure is now required to be HTTPS.",
  "sites_https_status": "Met",
  "sites_password_security_justification": "Confirmed N/A: this project stores no user passwords and operates no authenticating site.",
  "sites_password_security_status": "N/A",
  "static_analysis_common_vulnerabilities_justification": "CodeQL (`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/.github/workflows/codeql.yml`) and OpenSSF Scorecard run on every push/PR. Bandit runs on every push and pull request at medium severity and medium confidence (`.github/workflows/codeql.yml`, the `Analyze (Python)` job), and ruff's `S` rule set applies the same checks at lint time.",
  "static_analysis_common_vulnerabilities_status": "Met",
  "static_analysis_fixed_justification": "Findings are triaged and fixed before release.",
  "static_analysis_fixed_status": "Met",
  "static_analysis_justification": "CodeQL (results uploaded to GitHub code scanning), `.github/workflows/codeql.yml`. In addition to CodeQL: Bandit at medium severity and confidence on every push and pull request, and ruff's `S` (bandit) rule set at lint time.",
  "static_analysis_often_justification": "CodeQL runs on every push and PR to `main`, plus a weekly scheduled run.",
  "static_analysis_often_status": "Met",
  "static_analysis_status": "Met",
  "test_continuous_integration_justification": "GitHub Actions on every push and PR (`.github/workflows/ci.yml`). Python 3.10, 3.11, 3.12 and 3.13 on Linux, on every push and pull request.",
  "test_continuous_integration_status": "Met",
  "test_invocation_justification": "`pytest tests/`",
  "test_invocation_status": "Met",
  "test_justification": "Test suite under `tests/`, licensed with the project. How to run: `CONTRIBUTING.md#local-verification` and `.github/workflows/ci.yml`. Six test modules under `tests/`, organised by tool: one per MCP tool (`test_screen_payment_metadata.py`, `test_check_payment_policy.py`, `test_check_payment_replay.py`), plus `test_remote_url_validation.py` for the TLS gate, `test_parent_compatibility.py` guarding the parent version floor, and `test_e2e_inprocess_client.py` driving the server through a real in-process MCP client. 48 tests in total.",
  "test_most_justification": "Coverage is gated in CI at `--cov-fail-under=90` on statements, currently measuring **91.27%** across 48 tests. `tests/test_e2e_inprocess_client.py` drives the server end-to-end through a real in-process MCP client rather than calling the tool functions directly.",
  "test_most_status": "Met",
  "test_policy_justification": "URL: `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CONTRIBUTING.md#tests` \u2014 written policy that functional changes ship with tests and fixes ship with regression tests.",
  "test_policy_mandated_justification": "Formal written policy at `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CONTRIBUTING.md#tests`: functionality changes ship with tests; bug fixes include a regression test. Enforced in review and by the coverage gate.",
  "test_policy_mandated_status": "Met",
  "test_policy_status": "Met",
  "test_statement_coverage80_justification": "Enforced in CI, above the required floor: `.github/workflows/ci.yml` runs `pytest --cov=presidio_x402_mcp --cov-fail-under=90`, so a pull request dropping statement coverage below **90%** fails. Current measured coverage is **91.27%**.",
  "test_statement_coverage80_status": "Met",
  "test_status": "Met",
  "tests_are_added_justification": "Worked example: **PR #32** fixed the missing HTTPS requirement on the remote screening endpoint and shipped 19 tests in the same pull request, including prefix-match traps (`http://localhost.evil.example.com`) and a subprocess test proving the server refuses to start against a cleartext endpoint. **PR #31** is a second example: four regression tests shipped with the parent-floor fix, each verified to fail against the previous parent version.",
  "tests_are_added_status": "Met",
  "tests_documented_added_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/CONTRIBUTING.md#tests` states the policy that changes adding/modifying functionality ship with tests in the same PR.",
  "tests_documented_added_status": "Met",
  "updateable_reused_components_justification": "All reused components are standard package-index packages installed via the package manager (no vendored copies); Dependabot tracks updates.",
  "updateable_reused_components_status": "Met",
  "version_semver_justification": "URL: `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SEMVER.md` \u2014 documents the semver profile.",
  "version_semver_status": "Met",
  "version_tags_justification": "Every release is a git tag, SSH-signed and GitHub-verified.",
  "version_tags_signed_justification": "Every release is a git tag, SSH-signed with the org key and shown as Verified on GitHub.",
  "version_tags_signed_status": "Met",
  "version_tags_status": "Met",
  "version_unique_justification": "Semver per release, tagged.",
  "version_unique_status": "Met",
  "vulnerabilities_critical_fixed_justification": "Recent dependency criticals were closed by floor bumps within days. Confirmed. The most recent example is the parent redaction bypass: the fixed parent was released on 2026-08-02 and this project's floor was raised and released the same day, in 0.1.3.",
  "vulnerabilities_critical_fixed_status": "Met",
  "vulnerabilities_fixed_60_days_justification": "No known unpatched medium+ vulnerabilities. Dependabot plus dependency audit in CI. Confirmed: no known unpatched medium-or-higher vulnerability. `pip-audit` runs on every pull request, Dependabot covers both pip and GitHub Actions, and Dependabot security updates are enabled on the repository.",
  "vulnerabilities_fixed_60_days_status": "Met",
  "vulnerability_report_credit_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SECURITY.md#reporting-a-vulnerability` \u2014 reporters are credited by name in the published advisory and the CHANGELOG entry unless they request anonymity.",
  "vulnerability_report_credit_status": "Met",
  "vulnerability_report_private_justification": "URL: `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SECURITY.md#reporting-a-vulnerability` \u2014 private GitHub Security Advisory via the Security tab; acknowledgement and patch targets stated.",
  "vulnerability_report_private_status": "Met",
  "vulnerability_report_process_justification": "URL: `https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SECURITY.md#reporting-a-vulnerability`",
  "vulnerability_report_process_status": "Met",
  "vulnerability_report_response_justification": "Confirmed N/A: no vulnerability has been reported to this project by an external party. The findings addressed in 0.1.3 came from the project's own audit (`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SECURITY-AUDIT.md`) and from the parent library's release notes.",
  "vulnerability_report_response_status": "N/A",
  "vulnerability_response_process_justification": "`https://github.com/presidio-v/presidio-hardened-x402-mcp/blob/main/SECURITY.md#reporting-a-vulnerability` \u2014 private GitHub Security Advisory intake, acknowledgement and patch targets stated.",
  "vulnerability_response_process_status": "Met",
  "warnings_fixed_justification": "CI fails on any finding; `main` is clean.",
  "warnings_fixed_status": "Met",
  "warnings_justification": "Lint/warnings enforced in CI. ruff, run in CI as `ruff check src/ tests/ fuzz/` and `ruff format --check`, with the job failing on any finding.",
  "warnings_status": "Met",
  "warnings_strict_justification": "ruff with `select = [\"E\",\"F\",\"W\",\"I\",\"N\",\"UP\",\"S\",\"B\",\"A\",\"C4\",\"SIM\",\"TCH\"]` \u2014 well beyond defaults, and including the `S` (bandit) security rule set. Documented exclusions are narrow and justified in `pyproject.toml`: `S101` (assert is the idiom in tests) and `S603`/`S607` (subprocess calls use fixed, non-shell argv). CI fails on any finding: `ruff check` and `ruff format --check` both gate the Lint job.",
  "warnings_strict_status": "Met"
}
